server:
  port: 10001
profiles: dev
management:
  server:
    port: 10002
  endpoints:
    web:
      base-path: /monitor
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
